Branching.nb --- считает бренчинги распадов с новыми (полученными при помощи ramC) спектральными функциями.
2pi_test_interpolation_plus_number of calculations test.nb --- проверка влияния количества точек в спектральной функции на конечный результат
check_branching.nb --- проверяет отношение бренчингов, полученных с новыми (ramC) и старыми (before, параметризированные) спектральными функциями
interf.nb --- проверяет интерференцию между каналами a1 и b1 в распаде на 4pi мезона
